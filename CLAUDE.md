# WisePay 프로젝트 규칙

## 현재 버전

**v1.7.1** (2026-07-01)

## 버전 관리 규칙 (SemVer)

`MAJOR.MINOR.PATCH` 형식으로 관리한다.

| 변경 규모 | 올릴 숫자 | 예시 |
|-----------|-----------|------|
| 버그 수정 | PATCH (세 번째) | 1.0.0 → 1.0.1 |
| 기능 추가 | MINOR (두 번째) | 1.0.0 → 1.1.0 |
| 대규모 변경 | MAJOR (첫 번째) | 1.0.0 → 2.0.0 |

버전 업 시 반드시 아래를 함께 갱신한다:
1. **CLAUDE.md** 상단 "현재 버전" 숫자
2. **README.md** 상단 버전 뱃지
3. **수정히스토리.md**에 `## vX.Y.Z` 릴리즈 기록 추가

## 파일 수정 시 필수 규칙

### 1. 수정한 파일 상단에 로그 기재 (매 수정마다)

**실제로 수정한 파일에만** 해당 파일 상단(주석 처리 가능한 위치)에 최신 1줄로 갱신한다:

```
수정: YYYY-MM-DD HH:MM — [수정 내용 간단히]
```

예시:
```
수정: 2026-05-22 19:50 — freee 급여 CSV 업로드 UI 추가 + GAS importPayrolls 핸들러
```

- `index.html`도 **실제로 수정했을 때만** 이 규칙을 적용한다 (특별 취급 없음).

### 2. 커밋 & 푸시 (매 수정마다 즉시)

코드 변경이 있으면 바로 `git add` → **커밋 & 푸시**한다. 숙제.md / 수정히스토리.md 갱신 시점과 무관하게 즉시 진행한다.

### 3. 수정히스토리.md — 세션 종료 또는 명시적 요청 시에만

매 수정마다 건드리지 않는다. 세션 중 변경 내용을 기억해뒀다가, 아래 두 경우에 한 번에 몰아서 추가한다:
- 세션 종료 트리거("마무리하자", "오늘은 여기까지" 등)
- 사용자가 "히스토리 정리해줘"라고 명시적으로 요청할 때

삽입 위치: 파일 상단 헤더 **바로 아래** — 최신 항목이 항상 위에 오도록.

```
## YYYY-MM-DD HH:MM JST
- [수정 내용 간단히]
```

### 기타 주의사항

- 날짜/시간은 반드시 **JST(일본 표준시, Asia/Tokyo)** 기준으로 기록한다 — 시스템 또는 인터넷으로 정확한 시각을 확인할 것
- 코드에서 날짜/시간을 다룰 때는 `Utilities.formatDate(new Date(), "Asia/Tokyo", "yyyy-MM-dd HH:mm:ss")` 사용
- 내용은 한국어로 작성
- 처음 기동 시 Claude Code CLI를 최신 버전으로 전역 업데이트하고 시작한다 (실패 시 skip)

## 숙제(작업 인수인계) 기록 규칙

집 ↔ 회사 등 기기를 오가며 작업을 끊김 없이 이어가기 위한 인수인계 파일이 `숙제.md`이다.

- **매 수정마다 건드리지 않는다.** 세션 종료 트리거 또는 사용자가 **"숙제 기록"**(또는 "숙제에 기록", "숙제 정리해" 등 유사 표현)이라고 할 때 그 세션 전체 내용을 `숙제.md`에 **덮어써서** 정리한다.
- 다른 기기에서 새 Claude 세션이 **이 파일만 읽어도 작업을 이어갈 수 있을 만큼 누락 없이** 작성한다. 포함할 것:
  1. 지금 무슨 작업 중인지(목적)
  2. 확정된 결정·모델·규칙 (되돌리면 안 되는 합의 사항)
  3. 오늘(이번 세션) 변경한 것 — 무엇을·왜·어떻게 (변경 파일·커밋 해시)
  4. 검증 기준값 (정답으로 삼아야 할 수치/케이스가 있으면)
  5. 남은 할 일(TODO) — 우선순위와 함께
  6. 주의/보류 사항
- 완료된 TODO는 `숙제.md`에서 삭제한다 (완료 표시만 남기지 말고 항목 자체를 지운다).
- 상단에 "최종 갱신: YYYY-MM-DD HH:MM JST"를 적는다.
- 작성 후 **반드시 커밋 & 푸시**한다 (다른 기기에서 `git pull`로 받을 수 있게).
- 새 세션 시작 시 `숙제.md`가 있으면 먼저 읽고 맥락을 파악한 뒤 작업한다.

## "마무리" 한 방 규칙 (작업 종료 트리거)

사용자가 **"마무리하자"**, **"오늘은 여기까지"**, **"내일 하자"**, **"끝내자"** 등 **작업 종료를 알리는 표현**을 하면, 추가 확인 없이 **아래를 원스텝으로 한 번에** 실행한다:

1. 진행 중이던 변경/논의 내용을 `숙제.md`에 **덮어써서** 정리한다 (위 "숙제 기록 규칙"의 6개 항목 포함, 미완료 TODO·다음에 할 일 명시).
2. 이번 세션에서 변경한 내용을 `수정히스토리.md` 상단에 한 번에 추가한다.
3. 변경된 **모든 파일을 저장**하고 `git add -A` → **커밋 & 푸시**한다.

→ 목적: 집 ↔ 회사 어디서든 이 말 한마디로 작업분이 안전하게 원격에 올라가, 다음 기기에서 `git pull` 후 바로 이어갈 수 있게 한다.

## 수정 완료 후 출력 형식

수정이 완료되면 반드시 아래 형식으로 결과를 출력한다:

```
### 요구사항
[사용자가 요청한 내용을 그대로 또는 요약하여 기재]

### 수정 내용
[변경된 파일과 변경 내용을 구체적으로 기재]
```

예시:
```
### 요구사항
임금대장 인쇄 시 스크롤바가 출력된다 — 숨김 처리해 줘.

### 수정 내용
- css/styles.css: @media print에 .annual-scroll-wrap::-webkit-scrollbar { display:none } 추가
- js/app.js: beforeprint 이벤트에서 overflow:visible 인라인 스타일 적용
```
