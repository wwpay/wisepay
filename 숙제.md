# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-06-18 21:45 JST**

---

## 1. 지금 무슨 작업 중인가 — 유급휴가(연차) 계산 로직 정비

WisePay의 유급휴가 잔여/소멸 계산을, 사용자가 엑셀로 관리하던 **원본과 정확히 일치**하도록 정비하는 작업. 오늘 모델을 확정하고 핵심 구현을 끝냈음. (모두 커밋·푸시 완료)

---

## 2. 확정된 계산 모델 — 날짜순 FIFO 시뮬레이션 (oldest-first) ★되돌리지 말 것★

- **규칙**: 각 사용은 **그 사용일에 살아있는(이미 발생됨 & 아직 미소멸) 가장 오래된 발생분부터** 차감한다.
- **발생분 소멸일** = `grant_year + 2`년의 1월 1일 0시. (예: 2024 발생분 → 2026-01-01 00:00 소멸)
- **핵심 성질**: 소멸 임박한 옛 발생분을 먼저 쓰므로, "곧 사라질 휴가를 쓴 것"은 **이월 잔여를 줄이지 않는다.**
  - 예) 2024/1/1 입사·2025/7/1에 1일 사용 → 그 1일은 2024 발생분(곧 소멸)에서 빠짐 → 현재 잔여 **30** (29 아님).
- **발생분 종류**: 초기발생(date=입사일, grant_year=입사년) / 연간발생(date=YYYY-01-01, grant_year=YYYY). 둘 다 `grant_year = year(date)`.
- 폐기된 옛 모델들: ① today 기준 grant_year 태깅, ② cap&spill. **다시 도입 금지.**

### 발생연도(grant_year) 취급
- **발생행 grant_year는 유지** (소멸연도 산출에 필요, FIFO가 이것만 읽음).
- **사용행 grant_year는 계산에 미사용** → 신규 사용행은 빈값(`''`)으로 기록하도록 바꿔둠.

---

## 3. 오늘 변경한 것 (커밋·푸시 완료)

| 커밋 | 내용 |
|---|---|
| `5cfb97c` | **1단계**: `js/vacation.js` `calcVacationSummary`를 날짜순 FIFO 시뮬레이션으로 전면 재작성 |
| `315d96d` | **2단계**: 사용행 grant_year 기록 중단(빈값) — addVacationUsage / PC모달 / mobile.html |

세부:
- `js/vacation.js` `calcVacationSummary`: 발생분 풀(pool)에 대해 사용을 날짜순으로 oldest-first 차감하는 `_simulate` / `_remainingAt` 구현.
  - grant_year는 발생행에서만 읽어 소멸연도 산출. 사용행 grant_year는 안 읽음.
  - **동작 변경**: 현재 잔여·올해 사용은 **오늘까지의 사용만** 반영(미래 예정 사용 제외). 미래 발생분도 제외. ← 엑셀 현재 스냅샷과 맞추기 위함. **(주의: 아래 TODO 2와 직결)**
- `_resolveGrantYear`, `_buildGrantMap`: 호출처가 사라져 **deprecated**(코드는 보존, 주석 표시).
- `addVacationGrant`(발생행 생성)는 그대로 — grant_year 계속 기록.

---

## 4. 검증 기준값 (엑셀 원본 = 정답) ★수정 후 반드시 이 값과 일치 확인★

기준일 **2026-06-18**, 형식 = `1/1기준잔여 / 올해사용 / 남은연차 / 소멸예정`

| 사원 | 발생분 | 정답값 |
|---|---|---|
| **0002 정기석** | 2024=13, 2025=15, 2026=15 | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | 2025=14, 2026=15 | **18.0 / 8.0 / 10.0 / 0.0** |

- 라이브 브라우저에서 두 사원 모두 위 값 일치 **확인 완료**.
- 회귀 검증법: `calcVacationSummary`를 Node로 떼어내 위 데이터로 돌려 일치 확인(임시 스크립트는 매번 만들고 지움, 커밋 안 함).

---

## 5. 남은 할 일 (TODO)

1. ~~**[선택·시트 정리]** GAS 일회성 함수 `clearUsageGrantYear()` 실행~~ → **완료** (사용자가 Apps Script 편집기에서 실행함).

2. ~~**[핵심] 미래 예정분 반영**~~ → **완료 (v1.5.0, A안 채택)**.
   - `calcVacationSummary`의 현재 잔여·올해 사용을 **미래 예정 사용(오늘 포함)까지 반영**하도록 변경(`usageCutoff` 무제한). 미래 계획 휴가를 입력하면 "남은 연차"에서 미리 차감됨.
   - 추가로 **"미래 N일 포함"** 캡션을 "올해 사용" 옆/아래에 표시(PC 상세·PC 카드·모바일 상세·모바일 카드). 신규 반환값 `futureUsed` = 오늘 포함 미래 예정 사용일수.
   - 권한 구조(과거 열람만/미래 입력·삭제)는 기존 구현 그대로 — 이번 변경과 무관.
   - 검증값(4번, 0002·0019)은 미래 사용이 없어 그대로 유지됨.

3. **[다음 작업·UI 개선] "미래 N일 포함" 표시 다듬기** — 내일 회사에서 이어서 할 것. (기능은 동작하나 표시가 미흡)
   - **문제 A (레이아웃 깨짐)**: PC 카드뷰에서 "올해 사용 7.0일 (미래0.0일)"처럼 한 줄에 덧붙은 캡션 때문에 카드 전체 정렬이 깨짐. → 캡션을 값 옆 인라인이 아니라 **아래 줄/별도 위치**로 빼거나, `0.0일`일 때는 **숨김** 처리 검토.
   - **문제 B (상세도 보기 안 좋음)**: PC 상세 4-스탯 그리드 아래 `미래 0.0일 포함`도 정돈 안 됨. 정렬·여백·노출 조건 재검토.
   - **문제 C (문구 직관성)**: 일반 사원이 "미래 N일 포함"을 처음 보고 의미를 모름. → **더 쉬운 문구**로 교체하거나 **툴팁/설명** 추가. (예: "예정 포함" / "신청한 미래 휴가 N일 반영됨" 등 — 문구 확정 필요) ※ wiseadmin/사원 권한별로 문구를 달리할지도 고려.
   - **노출 조건 합의 필요**: `futureUsed === 0`일 때도 항상 표시할지, 0이면 숨길지 → 사용자와 결정 후 구현.
   - 관련 위치: PC 카드 `js/vacation.js` `renderVacationCards`(올해 사용 행), PC 상세 `renderVacationDetail`(`.vac-detail-stat-sub`), 모바일 `mobile.html`(카드 ~1441줄 / 상세 ~1779줄). CSS `.vac-detail-stat-sub`(css/styles.css).
   - 계산 자체(`calcVacationSummary`의 `futureUsed`, 미래 반영)는 **확정·완료** — 표시만 손보면 됨.

4. **[보류]** 작업트리에 사용자 본인이 정리 중인 무관 변경들 — DEPLOY.md·copilot-instructions.md 삭제, md_files/, .windsurfinstructions, vacation_input.js·vacation_fix_0017_to_0019.js(테스트 스크립트), .claude/settings.local.json. **건드리지 말 것**(사용자가 직접 정리).

---

## 6. 참고 — GAS 일회성 스니펫 (clearUsageGrantYear)

```javascript
// 일회성: 사용행(used>0)의 grant_year만 비움. 발생행(used=0)은 건드리지 않음.
function clearUsageGrantYear() {
  var sh = getSheet(SHEET_VACATION);
  if (sh.getLastRow() < 2) { Logger.log('데이터 없음'); return; }
  var vals = sh.getDataRange().getValues();
  var hdr  = vals[0];
  var usedCol = hdr.indexOf('used');
  var gyCol   = hdr.indexOf('grant_year');
  if (usedCol < 0 || gyCol < 0) { Logger.log('컬럼 없음'); return; }
  var count = 0;
  for (var i = 1; i < vals.length; i++) {
    var used = parseFloat(vals[i][usedCol]) || 0;
    if (used > 0 && String(vals[i][gyCol]).trim() !== '') {
      sh.getRange(i + 1, gyCol + 1).setValue('');
      count++;
    }
  }
  Logger.log('사용행 grant_year 비움 완료: ' + count + '행');
}
```

---

## 7. 관련 파일·위치
- 계산 로직: `js/vacation.js` `calcVacationSummary` (날짜순 FIFO)
- 사용 입력: `js/vacation.js` `addVacationUsage` + 모달 저장부 / `mobile.html` 모바일 모달
- 발생 생성: `js/vacation.js` `addVacationGrant`, `initEmployeeVacation`
- 전체 작업 이력: `수정히스토리.md` (오늘치 2026-06-18 항목들)
- 모델 메모(회사 PC 로컬): `.claude/.../memory/project_vacation_model.md`
