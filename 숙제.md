# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-06-22 22:50 JST**

---

## 1. 현재 상태

오늘 세션(2026-06-22 야간) 작업 완료. 현재 진행 중인 작업 없음.

**오늘 완료한 것 (2026-06-22 야간)**

| 커밋 | 내용 |
|---|---|
| `738bf60` | `vacation.js` / `styles.css`: 유급휴가 달력 공휴일 빨간색 표시 (토+공휴일 겹칠 시 빨강 우선) |
| `41ed039` | `WisePay_GAS.gs`: 미사용 함수 3개 제거 (`calcInitialDays`, `ensureNewEmpVacation`, `detectNewEmployeesAndInitVacation`) |
| `0f7b651` | `WisePay_GAS.gs`: `backupWeekly`에 `유급휴가`·`공휴일` 시트 추가 (기존 누락) |
| `3bdab3b` | `js/backup.js`: Excel 백업(전체)에 `유급휴가` 시트 추가 |
| `.vscode/tasks.json` | VS Code 기동 시 git pull 타이밍 에러 수정 (3초 딜레이 + 재시도) |

---

## 2. ⚠️ 내일 회사에서 반드시 할 것

### (1) git pull
회사 VS Code 첫 기동 → 평소처럼 에러 날 수 있음 → **터미널에서 수동 `git pull` 1회** 실행  
→ 이후 재기동부터 에러 없이 자동 pull 됨

### (2) GAS 재배포
오늘 `WisePay_GAS.gs`를 두 번 수정했지만 **아직 GAS에 반영 안 됨**.  
> ⚠️ **GAS 반영 필요**: `WisePay_GAS.gs` 전체 복사 → Google Apps Script 편집기 `code.gs`에 붙여넣기 → 저장 → 배포 관리 → 새 버전으로 재배포
>
> 반영 내용:
> - `backupWeekly`: 유급휴가·공휴일 시트 백업 추가
> - 미사용 함수 3개 제거 (dead code 정리)

---

## 3. 되돌리지 말 것 — 유급휴가 계산 모델 (FIFO)

- **규칙**: 각 사용은 그 사용일 기준으로 **가장 오래된 발생분부터** 차감한다.
- **발생분 소멸일** = `grant_year + 2`년 1월 1일. (예: 2024 발생분 → 2026-01-01 소멸)
- **발생행 grant_year는 유지**, 사용행 grant_year는 빈값(`''`)으로 기록.
- **미래 예정 사용**은 당해년도 12/31까지만 올해 잔여에 반영. 내년 이후 예정은 제외.
- 폐기된 옛 모델(today 기준 태깅, cap&spill) **다시 도입 금지.**

---

## 4. 검증 기준값 (유급휴가 수정 시 반드시 확인)

기준일 **2026-06-18**, 형식 = `1/1기준잔여 / 올해사용 / 남은연차 / 소멸예정`

| 사원 | 정답값 |
|---|---|
| **0002 정기석** | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | **18.0 / 8.0 / 10.0 / 0.0** |

---

## 5. 주요 파일 위치

| 기능 | 파일 |
|---|---|
| 유급휴가 계산 로직 | `js/vacation.js` — `calcVacationSummary` |
| 유급휴가 달력 렌더링 | `js/vacation.js` — `_renderVacCalendar` |
| 유급휴가 사용 입력 | `js/vacation.js` — `addVacationUsage` |
| 유급휴가 발생 생성 | `js/vacation.js` — `addVacationGrant`, `initEmployeeVacation` |
| Excel 전체 백업 | `js/backup.js` — `downloadBackupExcel` (사원정보·급여데이터·보험료율이력·유급휴가 4시트) |
| 급여명세 권한 제어 | `js/auth.js` — `applyViewerRestrictions` |
| 비번 눈 아이콘 | `js/auth.js` — `togglePwEye(inputId, btnId)` |

---

## 6. 주의·보류 사항

- 공휴일 데이터는 GAS 시트에 **2035년까지** 하드코딩. 이후 연도는 `setupHolidaySheet()` 배열에 추가 후 GAS에서 직접 실행하면 됨 (배포 불필요).
