# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-06-22 18:13 JST**

---

## 1. 현재 상태

오늘 세션 작업 완료. 현재 진행 중인 작업 없음.

**오늘 완료한 것 (2026-06-22)**

| 커밋 | 내용 |
|---|---|
| `a95105b` | 비번 변경·재설정 입력란 5곳 + 사원 비번 재설정 폼에 password visibility toggle(눈 아이콘) 추가 |
| `e9c668b` | GAS `changePassword` 오류 메시지 한국어 전용 → 한일 이중 언어(`<br>`) |
| `845bbda` | 비번 변경 오류 메시지 전체 `innerHTML`+`<br>` 2줄 표기 통일 (textContent→innerHTML) |
| `9071184` | `cpw-err` min-height 2줄 확보(버튼 밀림 방지), result.error 폴백 복원 |
| `5b8f305` | 사원 데이터없음 상태: 공제 항목·합계 0 표시 → 공란 처리 |
| `167b420` | 사원 데이터없음 상태: 차인지급액 ¥0 + 前月比 ─ ±0 유지 (레이아웃 점프 방지) |

**버전:** v1.6.1 → **v1.7.0** (MINOR: 눈 아이콘 신규 기능)

---

## 2. 되돌리지 말 것 — 유급휴가 계산 모델 (FIFO)

- **규칙**: 각 사용은 그 사용일 기준으로 **가장 오래된 발생분부터** 차감한다.
- **발생분 소멸일** = `grant_year + 2`년 1월 1일. (예: 2024 발생분 → 2026-01-01 소멸)
- **발생행 grant_year는 유지**, 사용행 grant_year는 빈값(`''`)으로 기록.
- **미래 예정 사용**은 당해년도 12/31까지만 올해 잔여에 반영. 내년 이후 예정은 제외.
- 폐기된 옛 모델(today 기준 태깅, cap&spill) **다시 도입 금지.**

---

## 3. 검증 기준값 (유급휴가 수정 시 반드시 확인)

기준일 **2026-06-18**, 형식 = `1/1기준잔여 / 올해사용 / 남은연차 / 소멸예정`

| 사원 | 정답값 |
|---|---|
| **0002 정기석** | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | **18.0 / 8.0 / 10.0 / 0.0** |

---

## 4. 주요 파일 위치

| 기능 | 파일 |
|---|---|
| 유급휴가 계산 로직 | `js/vacation.js` — `calcVacationSummary` |
| 유급휴가 사용 입력 | `js/vacation.js` — `addVacationUsage` / `mobile.html` 모바일 모달 |
| 유급휴가 발생 생성 | `js/vacation.js` — `addVacationGrant`, `initEmployeeVacation` |
| 급여명세 권한 제어 | `js/auth.js` — `applyViewerRestrictions` |
| 비번 눈 아이콘 범용 함수 | `js/auth.js` — `togglePwEye(inputId, btnId)` |
| 사원 데이터없음 공란 처리 | `js/payroll.js` — `loadPayrollForm` 조기 리턴 블록 (약 342행) |
| 비번 변경 오류 표시 | `js/change-password.js` — `doChangePassword` |

---

## 5. GAS 재배포 완료 여부

- 오늘(2026-06-22) GAS 재배포 **완료** 확인.
- 변경 내용: `changePassword` / `updatePassword` 오류 메시지 → `<br>` 2줄 표기

---

## 6. 주의·보류 사항

없음.
