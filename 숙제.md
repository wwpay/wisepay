# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-06-25 00:10 JST**

---

## 1. 현재 상태

2026-06-24 세션 작업 완료. 비밀번호 재설정(이메일 링크 방식) 기능 **완성·배포 완료**.
다음 세션에서 할 일: **유급휴가 달력 동기화 버그** (아래 2번 TODO).

**오늘(2026-06-24~25) 완료한 것**

| 커밋 | 내용 |
|---|---|
| (이전) | GAS: 비번 분실 → 이메일 링크 방식 전환 (`requestPasswordReset` + `confirmPasswordReset`), `confirmReset` doGet 액션 추가 |
| (이전) | auth.js/index.html: 비번 분실 모달 + 임시 비번 결과 모달 구현, reset_token URL 감지 |
| `5383d7a` | 임시 비번 모달 UI: 한글 병기·복사 버튼·설명 글씨 개선·디버그 제거 |
| `bb726e6` | 비번 분실 모달 취소 버튼 줄바꿈 → 사선(/) 구분으로 통일 |
| `c4ba350` | auth.js 캐시버스팅 쿼리(`?v=...`) 추가 + 결과 모달 폭 320px |
| `ca476b2` | 결과 모달 폭 360px + `word-break:keep-all` (일본어 줄바꿈 개선) |
| `636a7af` | 파비콘 `<link rel="icon">` 추가 (index/mobile) — 탭 아이콘 표시 |

---

## 2. TODO — 다음에 할 것

### ⚠️ (우선) 유급휴가 달력 동기화 버그

**증상**: 유급휴가 상세 화면에서 **연간 리스트** 항목을 클릭하면 해당 날짜에 동그라미(highlight)가 표시되는데, 지금 달력에 표시 중인 달과 **다른 달**의 항목을 눌러도 **월은 안 바뀌고** 현재 달의 같은 "일(日)"에만 동그라미가 찍힘.

**원하는 동작**: 다른 달 항목을 클릭하면 **달력도 그 달로 전환**된 뒤 정확한 날짜에 동그라미 표시.

**원인 위치**: [js/vacation.js](js/vacation.js) `_highlightVacListItem(idx)` (약 863줄)
- 클릭 항목의 `r.date`에서 **일(day)만** 뽑아 `dayNum` 계산
- 셀 인덱스를 **현재 표시 중인** `_vacDetailYear` / `_vacDetailMonth`의 1일 요일 기준으로 계산 → 월 전환 안 함

**수정 방향**:
`_highlightVacListItem`에서 `r.date`의 연/월을 파싱해, `_vacDetailYear`/`_vacDetailMonth`와 다르면
1. `_vacDetailYear` / `_vacDetailMonth`를 그 날짜로 변경
2. `_renderVacNavBar()` + `_renderVacCalendar()` 호출해 달력을 해당 월로 전환
3. 그 뒤 달력 highlight 적용
- 주의: 연간 모드에서 리스트(`_renderVacList`)는 다시 그리지 말 것(`active` 클래스 사라짐). 리스트는 두고 **달력만** 전환.

---

## 3. 비밀번호 재설정 시스템 (완성 — 되돌리지 말 것)

**흐름**:
- 로그인 화면 "パスワード分失 / 비밀번호 분실" → 모달에 ID 입력 → 확인
- **admin(wiseadmin)**: GAS가 1회용 UUID 토큰 생성 → `reset_tokens` 시트 저장 → `ADMIN_EMAIL`(lucky4694@gmail.com)로 링크 메일 발송 (1시간 유효)
  - 링크: `https://wwpay.github.io/wisepay/?reset_token=토큰`
  - 클릭 시 `confirmReset` 호출 → 8자리 임시 비번 생성·화면 표시, 토큰 `used=true` 처리(**1회용**)
- **employee/viewer**: ADMIN_EMAIL로 "[ID] ([이름])님이 비밀번호 분실을 신고했습니다" 통지 메일 → 관리자가 수동 처리

**중요 사항**:
- GET 호출은 반드시 `gasRequest()`(JSONP) 사용 — `fetch()`는 CORS로 막힘
- reset_token 감지는 `window.addEventListener('load', ...)` 안에서 (gas.js가 auth.js보다 늦게 로드돼 `gasRequest`가 parse 시점엔 없음)
- 메일 링크 URL은 **반드시 GitHub Pages** (`https://wwpay.github.io/wisepay/`) — netlify 쓰지 말 것
- `users` 시트에 사원 계정 만들 때 **이름 컬럼 포함** (통지 메일 괄호에 이름 표시용)
- `reset_tokens` 시트 컬럼: token, user_id, created_at, expires_at, used

---

## 4. 되돌리지 말 것 — 유급휴가 계산 모델 (FIFO)

- **규칙**: 각 사용은 그 사용일 기준으로 **가장 오래된 발생분부터** 차감한다.
- **발생분 소멸일** = `grant_year + 2`년 1월 1일. (예: 2024 발생분 → 2026-01-01 소멸)
- **발생행 grant_year는 유지**, 사용행 grant_year는 빈값(`''`)으로 기록.
- **미래 예정 사용**은 당해년도 12/31까지만 올해 잔여에 반영. 내년 이후 예정은 제외.
- 폐기된 옛 모델(today 기준 태깅, cap&spill) **다시 도입 금지.**

---

## 5. 검증 기준값 (유급휴가 수정 시 반드시 확인)

기준일 **2026-06-18**, 형식 = `1/1기준잔여 / 올해사용 / 남은연차 / 소멸예정`

| 사원 | 정답값 |
|---|---|
| **0002 정기석** | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | **18.0 / 8.0 / 10.0 / 0.0** |

---

## 6. 주요 파일 위치

| 기능 | 파일 |
|---|---|
| 유급휴가 계산 로직 | `js/vacation.js` — `calcVacationSummary` |
| 유급휴가 달력 렌더링 | `js/vacation.js` — `_renderVacCalendar` (약 727줄) |
| 유급휴가 리스트 렌더링 | `js/vacation.js` — `_renderVacList` (약 800줄) |
| **유급휴가 리스트 클릭 → 달력 highlight** | `js/vacation.js` — `_highlightVacListItem` (약 863줄) ← **버그 위치** |
| 유급휴가 월/년 네비 | `js/vacation.js` — `vacYearPrev/Next`, `vacSetMonth`, `vacGotoToday` |
| 비번 재설정 모달/토큰 감지 | `js/auth.js` — `openPwResetModal`, `submitPwReset`, `checkResetToken`(load 이벤트) |
| 비번 재설정 GAS | `WisePay_GAS.gs` — `requestPasswordReset`, `confirmPasswordReset` |
| 급여명세 권한 제어 | `js/auth.js` — `applyViewerRestrictions` |
| 사용자 계정 데이터 | GAS Spreadsheet `users` 시트 / 토큰: `reset_tokens` 시트 |

---

## 7. 주의·보류 사항

- **GAS 수정 시 반드시**: WisePay_GAS.gs 복붙 → 저장 → **새 버전으로 재배포**(권한 Everyone) 해야 반영됨.
- **JS/HTML 캐시**: GitHub Pages CDN + 브라우저가 `index.html`을 캐시함. JS 변경 후 안 보이면 빈 로그인 화면에서 **Ctrl+Shift+R** 먼저 → 그 후 동작. auth.js는 `?v=날짜` 캐시버스팅 사용 중(수정 시 버전 문자 갱신).
- 공휴일 데이터는 GAS 시트에 **2035년까지** 하드코딩. 이후 연도는 `setupHolidaySheet()` 배열에 추가 후 GAS에서 직접 실행(배포 불필요).
- 오늘 날짜 달력 표시: `box-shadow: 0 0 0 2px #D97706` + `border-radius:6px` (`.vac-day-used`와 동일 크기).
