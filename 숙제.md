# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-06-20 08:02 JST**

---

## 1. 지금 무슨 작업 중인가 — 급여명세서 표준 보수 월액 권한별 노출 제어

급여 명세서 상세 화면에서 **표준 보수 월액** 항목을 관리자(wiseadmin)만 보이도록 설정.
세리사(관리자 외 관리 계정)와 일반 사원에게는 해당 섹션을 **빈 칸(공간 유지)** 으로 처리해 레이아웃이 깨지지 않도록 한다.

---

## 2. 확정된 계산 모델 — 날짜순 FIFO 시뮬레이션 (oldest-first) ★되돌리지 말 것★

- **규칙**: 각 사용은 **그 사용일에 살아있는(이미 발생됨 & 아직 미소멸) 가장 오래된 발생분부터** 차감한다.
- **발생분 소멸일** = `grant_year + 2`년의 1월 1일 0시. (예: 2024 발생분 → 2026-01-01 00:00 소멸)
- **핵심 성질**: 소멸 임박한 옛 발생분을 먼저 쓰므로, "곧 사라질 휴가를 쓴 것"은 **이월 잔여를 줄이지 않는다.**
  - 예) 2024/1/1 입사·2025/7/1에 1일 사용 → 그 1일은 2024 발생분(곧 소멸)에서 빠짐 → 현재 잔여 **30** (29 아님).
- **발생분 종류**: 초기발생(date=입사일, grant_year=입사년) / 연간발생(date=YYYY-01-01, grant_year=YYYY). 둘 다 `grant_year = year(date)`.
- 폐기된 옛 모델들: ① today 기준 grant_year 태깅, ② cap&spill. **다시 도입 금지.**

### 발생연도(grant_year) 취급
- **발생행 grant_year는 유지** (소멸연도 산출에 필요, FIFO가 이것만 읽음).
- **사용행 grant_year는 계산에 미사용** → 신규 사용행은 빈값(`''`)으로 기록.

### 미래 예정 사용 반영 규칙 (v1.5.1 확정)
- 미래 예정 사용은 **당해년도 12/31까지만** 올해 잔여 계산에 반영. 내년 이후 예정은 제외.

---

## 3. 완료된 작업 이력

| 커밋 | 내용 |
|---|---|
| `5cfb97c` | 날짜순 FIFO 시뮬레이션으로 전면 재작성 |
| `315d96d` | 사용행 grant_year 기록 중단(빈값) |
| `26457ee` | 미래 예정 사용을 당해년도(12/31)로 한정 (v1.5.1) |
| `cb70680` | 유급휴가 표시 수식형 통일 + 등록 안내 팝업 (v1.6.0) |
| `838872c` | 유급 카드 세로형 복귀(소멸예정 연한배경) + 상세 연산기호 |
| `ad685f6` | 미래(예정) 등록일 연보라색 구분 (달력·월탭·리스트) |
| `9913f5c` | admin 유급휴가 전체 0일 버그 수정 (JSONP 콜백 충돌) [v1.6.1] |

---

## 4. 검증 기준값 (엑셀 원본 = 정답) ★수정 후 반드시 이 값과 일치 확인★

기준일 **2026-06-18**, 형식 = `1/1기준잔여 / 올해사용 / 남은연차 / 소멸예정`

| 사원 | 발생분 | 정답값 |
|---|---|---|
| **0002 정기석** | 2024=13, 2025=15, 2026=15 | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | 2025=14, 2026=15 | **18.0 / 8.0 / 10.0 / 0.0** |

---

## 5. 남은 할 일 (TODO)

1. **[진행 중] 급여명세서 상세 — 표준 보수 월액 권한별 노출 제어**
   - 관리자(wiseadmin): 표준 보수 월액 표시 (현행 유지)
   - 세리사 / 일반 사원: 해당 섹션 **빈 칸** 처리 (레이아웃 유지, 값만 숨김)
   - 관련 파일: 급여명세서 상세 렌더링 부분 (확인 필요)

2. **[보류]** 작업트리에 사용자 본인이 정리 중인 무관 변경들 — DEPLOY.md·copilot-instructions.md 삭제, md_files/, .windsurfinstructions, vacation_input.js·vacation_fix_0017_to_0019.js(테스트 스크립트), .claude/settings.local.json. **건드리지 말 것**(사용자가 직접 정리).

---

## 6. 관련 파일·위치
- 계산 로직: `js/vacation.js` `calcVacationSummary` (날짜순 FIFO)
- 사용 입력: `js/vacation.js` `addVacationUsage` + 모달 저장부 / `mobile.html` 모바일 모달
- 발생 생성: `js/vacation.js` `addVacationGrant`, `initEmployeeVacation`
- 급여명세서: 상세 렌더링 위치 확인 필요
- 전체 작업 이력: `수정히스토리.md`
