# 숙제 (작업 인수인계)

> 집 ↔ 회사 기기를 오가며 작업을 이어가기 위한 파일.
> 다른 기기에서 새 Claude 세션을 시작하면 **이 파일을 먼저 읽고** 이어서 작업할 것.
> 최종 갱신: **2026-07-03 20:53 JST**

---

## 1. 현재 상태

2026-07-03 세션 작업 완료. **현재 진행 중인 작업 없음. 긴급 TODO 없음.**
현재 버전 **v1.8.0** (労働保険 年度更新 기능 추가로 MINOR 업).

**이번 세션(2026-07-03) 완료·검증한 것**

| 커밋 | 내용 |
|---|---|
| `becdccd` | 임금대장 기간 지정 — 인라인 → 모달 방식 전환 (시작·종료 연월 독립 선택, 12개월 초과 시 toast 경고) |
| `c426a0c` | 算定基礎届를 「신고서 작성 / 申告書作成」 신규 섹션으로 분리, 메뉴 한/일 언어전환 |
| `f6888ae` | **労働保険 年度更新** 화면 신규 추가 (js/roudou.js) |
| `11373ff` | 労働保険 申告済概算保険料額 자동연계 |
| `cab26ac` | v1.8.0 릴리즈 (CLAUDE.md·README·수정히스토리) |

→ **労働保険 年度更新은 실물 신고서와 전항목 일치 검증 완료** (確定144,357 / 不足507 / 納付額136,779).

---

## 2. 労働保険 年度更新 — 확정 사양 (되돌리지 말 것)

**파일**: `js/roudou.js` (전체 로직), index.html(page-roudou + 모달 2개), app.js(라우팅), i18n.js(메뉴 번역)
**접근권한**: wiseadmin 전용 (算定基礎届과 동일 정책, wisejp·세리사·사원 비노출) — **wisejp도 안 보이는 게 맞음(확정)**

### 집계 기준
- 신고연도 **Y**(서기, 概算 대상 = 令和(Y-2018)年度)
- **確定 산정기간**: 지급일 (Y-1).4~Y.3 → WisePay **귀속월 (Y-1).3月分 ~ Y.2月分 (12개월)**
- **概算 산정기간**: Y.4~(Y+1).3, 기본값 = 확정 임금총액과 동일, 수동조정 가능
- **지급확정(`paidYMs`) 스냅샷만** 집계. `calcPayrollData().totalPay`(통근수당 포함) = 労働保険 賃金総額
- **対象者**: config `roudou_exclude_nos`(役員扱い 사번=지점장 0002)를 賃金総額·인원(④⑤)에서 제외

### 계산 (記入例4 同額 케이스 — 절대 변경 금지)
- 労災기초액 == 雇用기초액이면 **同額 모드**: `保険料 = floor(千円단위賃金総額 × 합산요율)` **한 번에** 곱셈 (労災分/雇用分 각각 계산 후 합산 방식 **금지** — 절사차 발생)
- 労災分/雇用分 행은 공란(사선). 労災≠雇用이면 각각 계산 후 労働保険料=합계
- `senKiri(yen)=floor(yen/1000)`, `hokenryo(sen,rate)=floor(sen*rate)`
- 一般拠出金 = floor(労災기초액_千円 × ippan요율)
- 정산: 差引=確定-申告済概算 → 差引>0 不足(ハ)/差引<0 充当(イ). (ニ)今期労働保険料=概算+差引, (ヘ)=一般拠出金, (ト)=(ニ)+(ヘ). 40만엔 미만 一括(延納なし)
- 인원 ④⑤ = Σ12개월 인원/12, 절사, 0이면 1

### 요율 (config 연도접미사 키, 화면에서 수정 가능)
- 確定용 = 前年度 키 `roudou_rate_{Y-1}_total/rousai/koyo`, `roudou_ippan_kyoshutsu_{Y-1}`
- 概算용 = 当年度 키 `roudou_rate_{Y}_total/rousai/koyo`
- 당년도 값 없으면 전년도 값 폴백. 기본값: 確定 17.5(労災3.0+雇用14.5)/一般0.02, 概算 16.5(3.0+13.5)
- OCR 카드 인쇄요율 우선 주의문구 상시 표시

### 申告済概算保険料額 자동연계
- `getShinkokuZumi(Y)` 우선순위: **수동입력 `roudou_shinkoku_zumi_gaisan_{Y}` > 전년도 概算 `roudou_gaisan_ryo_{Y-1}` 자동연계 > 미설정(0)**
- 화면 렌더 시 올해 概算保険料額을 `roudou_gaisan_ryo_{Y}`에 값 변동 시에만 1.5초 디바운스 GAS 저장 → 내년 자동연계

### config 키 목록 (범용 config 시트, GAS 수정 불필요 — getConfig/saveConfig 재사용)
`roudou_hokenbango`, `roudou_exclude_nos`, `roudou_rate_{연도}_total/rousai/koyo`, `roudou_ippan_kyoshutsu_{연도}`, `roudou_shinkoku_zumi_gaisan_{연도}`(수동), `roudou_gaisan_ryo_{연도}`(자동저장). 事業所정보는 算定基礎届의 `jigyosho_*` 공유.

---

## 3. 算定基礎届 (완성 — 되돌리지 말 것)

- `js/santei.js`, 「신고서 작성」 섹션, wiseadmin 전용
- 귀속월→지급월: 3월분→4월지급, 4월분→5월지급, 5월분→6월지급. 支払基礎日数 17일 이상만 평균 산입
- 事業所정보 config: `jigyosho_seiri/address/name/owner`

## 4. 비밀번호 재설정 시스템 (완성 — 되돌리지 말 것)

- 로그인 "비밀번호 분실" → ID 입력. **admin**: GAS가 1회용 UUID 토큰 → `reset_tokens` 시트 → ADMIN_EMAIL(lucky4694@gmail.com)로 링크메일(1시간). **employee/viewer**: 관리자에게 통지메일 수동처리
- GET은 반드시 `gasRequest()`(JSONP), fetch는 CORS차단. 메일 링크 URL은 **GitHub Pages**(`https://wwpay.github.io/wisepay/`) 고정
- `reset_tokens` 컬럼: token, user_id, created_at, expires_at, used

## 5. 유급휴가 계산 모델 FIFO (되돌리지 말 것)

- 각 사용은 사용일 기준 **가장 오래된 발생분부터** 차감. 발생분 소멸 = `grant_year+2`년 1/1
- 발생행 grant_year 유지, 사용행 grant_year 빈값. 미래 예정사용은 당해년도 12/31까지만 반영
- 폐기된 옛 모델(today 기준 태깅, cap&spill) 재도입 금지

### 검증 기준값 (기준일 2026-06-18, 형식: 1/1잔여 / 올해사용 / 남은연차 / 소멸예정)
| 사원 | 정답값 |
|---|---|
| **0002 정기석** | **19.0 / 7.0 / 12.0 / 0.0** |
| **0019 박수완** | **18.0 / 8.0 / 10.0 / 0.0** |

### 労働保険 검증 기준값
- 記入例4: 78,083千円 → 確定1,366,452 / 概算1,288,369 / 一般拠出金1,561
- 당사 실물: 確定144,357 / 不足507 / 納付額136,779

---

## 6. 주요 파일 위치

| 기능 | 파일 |
|---|---|
| 労働保険 年度更新 | `js/roudou.js` — `buildRoudouState`, `calcRoudouHoken`, `renderRoudou`, `getShinkokuZumi` |
| 算定基礎届 | `js/santei.js` — `renderSantei`, `recalcSanteiBlock` |
| 임금대장 기간지정 모달 | `js/history.js` — `openAnnualCustomModal`, `applyAnnualCustomRange` |
| 유급휴가 계산 | `js/vacation.js` — `calcVacationSummary` |
| 비번 재설정 | `js/auth.js` — `checkResetToken`(load 이벤트) / `WisePay_GAS.gs` — `requestPasswordReset` |
| 페이지 권한 제어 | `js/auth.js` — `canAccessPage`(admin만 santei/roudou 통과), `renderNavForRole` |
| config read/write | `js/gas.js` — `fetchConfig`, `saveConfigToGas` / `WisePay_GAS.gs` — `getConfigData`, `saveConfigData` |

---

## 7. 주의·보류 사항

- **GAS 수정 시**: WisePay_GAS.gs 복붙 → 저장 → **새 버전 재배포**(권한 Everyone). 단 이번 労働保険은 config 범용 API 재사용이라 GAS 변경 없었음
- **JS/HTML 캐시**: 변경 후 안 보이면 로그인 화면에서 **Ctrl+Shift+R**. auth.js는 `?v=날짜` 캐시버스팅
- **역원(役員) 컬럼 미추가**: 직원 마스터에 役員 필드 없음. 労災 제외는 config `roudou_exclude_nos`로만 처리 (현재 지점장 0002). 임원이 늘면 이 키에 사번 추가
- **상여 데이터 없음**: WisePay는 월급여만 저장. 労働保険 賃金集計表의 상여 3행은 수동입력
- 공휴일 데이터 GAS 시트 **2035년까지** 하드코딩
- 파일 수정 규칙: 매 수정마다 즉시 커밋·푸시. 수정히스토리·숙제.md는 세션 종료/명시요청 시에만 갱신. 날짜는 JST. 사용자 보고는 한국어
